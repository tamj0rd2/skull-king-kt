{
  "name": "frontend",
  "version": "1.0.0",
  "description": "",
  "engines" : {
    "npm" : ">=8.0.0 <9.0.0",
    "node" : ">=18.2.0 <19.0.0"
  },
  "scripts": {
    "build": "tsc && npm run clean && npm run build:game && npm run assets",
    "build:game": "esbuild src/game.ts --outdir=build/dist --bundle --minify --sourcemap",
    "assets": "cp -r assets/ build/dist",
    "clean": "rm -rf build/dist"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@tsconfig/recommended": "^1.0.3",
    "@types/node": "^20.8.7",
    "esbuild": "0.19.3",
    "typescript": "^5.2.2"
  }
}
