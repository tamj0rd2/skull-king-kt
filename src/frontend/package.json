{
  "name": "frontend",
  "version": "1.0.0",
  "description": "",
  "directories": {
    "test": "../test"
  },
  "scripts": {
    "build": "tsc && npm run build:game",
    "build:game": "esbuild game.ts --outfile=../main/resources/public/game.js --bundle --minify --sourcemap"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@tsconfig/recommended": "^1.0.3",
    "esbuild": "0.19.3",
    "typescript": "^5.2.2"
  }
}
